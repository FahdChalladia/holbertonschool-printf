.TH PRINTF 3 "March 2025" "1.0" "C Library"
.SH NAME
_printf \- Custom implementation of printf function
.SH SYNOPSIS
.B int _printf(const char *format, ...);
.SH DESCRIPTION
The 
.B _printf
function produces output according to the format string. It behaves similarly to the standard 
.B printf
function but only supports specific format specifiers.

The format string contains zero or more directives that describe how to format the output. Directives are composed of a percent sign ('%') followed by a format specifier. Supported format specifiers include:

.TP
.B \%c
Prints a single character.
.TP
.B \%s
Prints a string of characters.
.TP
.B \%d, \%i
Prints an integer number in decimal (base 10) format.
.TP
.B \%%
Prints a literal percent sign ('%').

.SH RETURN VALUE
The 
.B _printf
function returns the number of characters printed (excluding the null byte used to end output to strings). If an error occurs, it returns a negative value.

.SH EXAMPLES
.TP
\[
$ _printf("Hello, %s!\n", "World");
Hello, World!
\]
The above example prints "Hello, World!" followed by a newline.

.TP
\[
$ _printf("Value: %d\n", 42);
Value: 42
\]
The above example prints "Value: 42" followed by a newline.

.SH AUTHORS
The 
.B _printf
function was written by Fahd Challadia and the Holberton School team.

.SH SEE ALSO
.B printf(3)
